[project]
name = "qurious-crafting-log"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "flet>=0.24",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
